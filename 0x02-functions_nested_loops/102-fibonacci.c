#include <stdio.h>
/**
 *main - entry point
 *Return: always 0 (success)
 */

int main() {
    int first = 1;
    int second = 2;
    int i;
    int sum;

    printf("%i, ", first);
    printf("%i, ", second);
	for (i = 3; i <= 50; i++) 
	{
		sum = sum + i;
		printf("%i, ", next);
	}
	printf('\n');
    return (0);
}
